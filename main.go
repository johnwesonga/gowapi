package main

import (
	"github/johnwesonga/gowapi/cmd"
)

func main() {

	// fmt.Printf("Country: %s\nLat: %f", resp[0].Country, resp[0].Lat)

	// GetWeatherForCity(resp[0].Lat, resp[0].Lon)

	cmd.Execute()

}
